{
  "private": true,
  "type": "module",
  "name": "empty-project",
  "version": "0.1.7",
  "description": "Repo Template: vite, vitest, eslint.",
  "keywords": [],
  "author": "",
  "license": "MIT",
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=18.16.1"
  },
  "packageManager": "npm@9.6.2",
  "browserslist": "Chrome >= 112, ChromeAndroid >= 112, Edge >= 112, Firefox >= 112, FirefoxAndroid  >= 112,  Safari >= 16.4, iOS >= 16.4, not dead",
  "scripts": {
    "dev": "vite",
    "dev-host": "vite --host",
    "build": "vite build --emptyOutDir",
    "preview": "vite preview",
    "preview-host": "vite preview --host",
    "start": "vite --open",
    "lint": "eslint --ext .js --report-unused-disable-directives --env-info .",
    "test": "vitest run",
    "test-watch": "vitest",
    "test-coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^0.34.1",
    "@vitest/web-worker": "^0.34.1",
    "eslint": "^8.47.0",
    "eslint-config-prettier": "^9.0.0",
    "jsdom": "^22.1.0",
    "vite": "^4.4.9",
    "vitest": "^0.34.1"
  }
}
